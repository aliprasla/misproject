@model IEnumerable<PraslaBonnerWondwossenFinalProject.Models.Sales>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Shares)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NetProfit)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SharesLeft)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PurchaseId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Shares)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NetProfit)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SharesLeft)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PurchaseId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SalesId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SalesId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SalesId })
        </td>
    </tr>
}

</table>
