@model PraslaBonnerWondwossenFinalProject.Models.PurchasedStock

@{
    ViewBag.Title = "Sell";
}

<h2>Sell</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>PurchasedStock</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.PurchasedStockId)

        <p class="form-group">
            Amount of Shares: @Html.TextBox("Shares", null, new { @class = "form-control" }) <br />
            

        <p class="form-group">
            Date: @Html.TextBox("Date", null, new { @class = "form-control" }) <br />
            
                    <div class="col-md-offset-2 col-md-10">
                        <input type="submit" value="Save" class="btn btn-default" />
                    </div>
                </div>
            
            }

            <div>
                @Html.ActionLink("Back to List", "Index")
            </div>
